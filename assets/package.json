{
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.4",
    "postcss-import": "^14.0.2",
    "tailwindcss": "^2.2.19"
  },
  "scripts": {
    "deploy": "NODE_ENV=production tailwindcss --postcss --minify --input=css/app.css --output=../priv/static/assets/app.css && cd .. && rm -f _build/esbuild"
  },
  "dependencies": {
    "@tailwindcss/forms": "^0.3.4"
  }
}
